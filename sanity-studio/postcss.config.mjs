// Sanity Studio runs on Vite and should not inherit the Next.js/Tailwind PostCSS setup
// from the parent `ak/` project. Keeping this empty prevents Vite from loading `../postcss.config.mjs`.
export default {
  plugins: [],
};


